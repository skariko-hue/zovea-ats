<%- include('../partials/head', { title: 'Fout — Zovea ATS', req }) %>

<div class="rounded-2xl border bg-white p-6 shadow-sm">
  <h1 class="text-xl font-semibold">Er is een fout opgetreden</h1>
  <p class="mt-2 text-slate-600"><%= message %></p>

  <details class="mt-4 rounded-xl border bg-slate-50 p-4">
    <summary class="cursor-pointer text-sm font-medium">Technische details (voor beheer)</summary>
    <pre class="mt-3 text-xs overflow-auto"><%= (err && (err.stack || err.message)) || '' %></pre>
  </details>
</div>

<%- include('../partials/foot') %>

